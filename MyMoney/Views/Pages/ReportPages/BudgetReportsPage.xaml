<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyMoney.Views.Pages.ReportPages"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:Controls="clr-namespace:MyMoney.Views.Controls" x:Class="MyMoney.Views.Pages.ReportPages.BudgetReportsPage"
      d:DataContext="{d:DesignInstance {x:Type local:BudgetReportsPage}, IsDesignTimeCreatable=False}"
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="BudgetReportsPage"
      Loaded="Page_Loaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Margin="0,0,12,24">
            <ui:Card Margin="0,0,0,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ui:TextBlock Text="Month" Margin="0,0,0,8"/>
                    <ComboBox Grid.Row="1" Margin="0,0,4,0"/>

                    <ui:TextBlock Grid.Column="1" Grid.Row="0" Text="Year" Margin="4,0,0,8"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" Margin="4,0,0,0"/>
                </Grid>
            </ui:Card>
            
            <Controls:BudgetReportControl
                Title="{Binding ViewModel.ReportTitle}" IncomeItems="{Binding ViewModel.IncomeItems}"
                ExpenseItems="{Binding ViewModel.ExpenseItems}" ReportTotal="{Binding ViewModel.ReportTotal}"/>
        </StackPanel>



    </Grid>
</Page>
