<Page
    x:Class="MyMoney.Views.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:MyMoney.Helpers"
    xmlns:rbhelpers="clr-namespace:MyMoney.Helpers.RadioButtonConverters"
    xmlns:local="clr-namespace:MyMoney.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="SettingsPage"
    d:DataContext="{d:DesignInstance local:SettingsPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="600"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d"
>
    <Page.Resources>
        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
        <rbhelpers:RadioButtonEnumToBooleanConverter x:Key="RadioButtonEnumToBooleanConverter" />
    </Page.Resources>

    <StackPanel>
        <ui:CardExpander IsExpanded="False">
            <ui:CardExpander.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <ui:SymbolIcon
                        Symbol="Color24"
                        FontSize="28"
                        FontWeight="Medium"
                        Margin="0,0,12,0"
                        VerticalAlignment="Center"
                    />
                    <TextBlock
                        Grid.Column="1"
                        FontSize="20"
                        FontWeight="Medium"
                        Text="Personalization"
                        VerticalAlignment="Center"
                    />
                </Grid>
            </ui:CardExpander.Header>
            <ui:CardExpander.Content>
                <StackPanel>
                    <TextBlock Text="Theme" />
                    <RadioButton
                        Margin="0,8,0,0"
                        Command="{Binding ViewModel.ChangeThemeCommand, Mode=OneWay}"
                        CommandParameter="theme_light"
                        Content="Light"
                        GroupName="themeSelect"
                        IsChecked="{Binding ViewModel.CurrentTheme, Converter={StaticResource EnumToBooleanConverter}, 
                                            ConverterParameter=Light, Mode=OneWay}"
                    />
                    <RadioButton
                        Margin="0,8,0,0"
                        Command="{Binding ViewModel.ChangeThemeCommand, Mode=OneWay}"
                        CommandParameter="theme_dark"
                        Content="Dark"
                        GroupName="themeSelect"
                        IsChecked="{Binding ViewModel.CurrentTheme, Converter={StaticResource EnumToBooleanConverter}, 
                                            ConverterParameter=Dark, Mode=OneWay}"
                    />

                    <TextBlock Margin="0,12,0,0">Accent Color</TextBlock>
                    <ui:ListView
                        Margin="0,8,0,0"
                        ItemsSource="{Binding ViewModel.AccentColors, Mode=OneWay}"
                        SelectedItem="{Binding ViewModel.SelectedAccentColor}"
                    >
                        <ui:ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ui:ListView.ItemsPanel>
                        <ui:ListView.ItemTemplate>
                            <DataTemplate>
                                <Rectangle Width="48" Height="48" Fill="{Binding BaseBrush}" RadiusX="4" RadiusY="4">
                                    <Rectangle.ToolTip>
                                        <ToolTip Content="{Binding Name}" />
                                    </Rectangle.ToolTip>
                                </Rectangle>
                            </DataTemplate>
                        </ui:ListView.ItemTemplate>
                    </ui:ListView>

                    <ui:TextBlock
                        Margin="0,8,0,0"
                        Visibility="{Binding ViewModel.AccentColorAppliesOnRestartVisiblity}"
                        FontTypography="Caption"
                    >
                        New Accent color takes effect on restart
                    </ui:TextBlock
                    >
                </StackPanel>
            </ui:CardExpander.Content>
        </ui:CardExpander>
        <ui:CardExpander IsExpanded="False" Margin="0,16,0,0">
            <ui:CardExpander.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <ui:SymbolIcon Margin="0,0,12,0" Symbol="FolderArrowUp24" FontWeight="Medium" FontSize="28" />
                    <TextBlock Grid.Column="1" FontSize="20" FontWeight="Medium" Text="Backups" />
                </Grid>
            </ui:CardExpander.Header>
            <StackPanel>
                <RadioButton
                    GroupName="BackupMode"
                    Content="Manual"
                    Command="{Binding ViewModel.SaveBackupSettingsCommand}"
                    IsChecked="{Binding ViewModel.BackupMode, Converter={StaticResource RadioButtonEnumToBooleanConverter}, 
                                        ConverterParameter={x:Static rbhelpers:BackupModeRadioButtonGroup.Manual}}"
                />
                <RadioButton
                    Margin="0,8,0,0"
                    GroupName="BackupMode"
                    Content="Automatic"
                    Command="{Binding ViewModel.SaveBackupSettingsCommand}"
                    IsChecked="{Binding ViewModel.BackupMode, Converter={StaticResource RadioButtonEnumToBooleanConverter}, 
                                        ConverterParameter={x:Static rbhelpers:BackupModeRadioButtonGroup.Automatic}}"
                />
                <StackPanel
                    IsEnabled="{Binding ViewModel.BackupMode, Converter={StaticResource RadioButtonEnumToBooleanConverter}, 
                                       ConverterParameter={x:Static rbhelpers:BackupModeRadioButtonGroup.Automatic}}"
                >
                    <TextBlock Margin="0,12,0,0" Text="Backup location:" />
                    <WrapPanel Margin="0,8,0,0">
                        <ui:TextBox Width="400" Text="{Binding ViewModel.BackupLocation}" IsReadOnly="True" />
                        <ui:Button
                            Margin="4,0,0,0"
                            Content="..."
                            VerticalAlignment="Stretch"
                            Command="{Binding ViewModel.ChooseAutomaticBackupLocationCommand}"
                        />
                    </WrapPanel>

                    <TextBlock Margin="0,12,0,0" Text="Keep automatic backups for:" />
                    <ComboBox
                        Margin="0,8,0,0"
                        HorizontalAlignment="Left"
                        SelectedIndex="{Binding ViewModel.BackupDurationIndex}"
                        SelectionChanged="ComboBox_SelectionChanged"
                    >
                        <ComboBoxItem Content="4 days" />
                        <ComboBoxItem Content="1 week" IsSelected="True" />
                        <ComboBoxItem Content="2 weeks" />
                        <ComboBoxItem Content="1 month" />
                        <ComboBoxItem Content="3 months" />
                        <ComboBoxItem Content="Forever" />
                    </ComboBox>
                </StackPanel>
                <ui:Button Margin="0,12,0,0" Content="Backup now..." Command="{Binding ViewModel.BackupNowCommand}" />
                <ui:Button
                    Margin="0,12,0,0"
                    Content="Restore from backup..."
                    Command="{Binding ViewModel.RestoreFromBackupCommand}"
                />
            </StackPanel>
        </ui:CardExpander>

        <ui:Card Margin="0,16,0,24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ui:ImageIcon
                    Width="28"
                    Height="28"
                    Grid.RowSpan="2"
                    Source="pack://application:,,,/Assets/MyMoney-icon-256.png"
                />
                <TextBlock Margin="12,0,0,0" Grid.Row="0" Grid.Column="1" FontSize="16">MyMoney</TextBlock>
                <TextBlock Margin="12,0,0,0" Grid.Row="1" Grid.Column="1">© 2024-2025 Jordan Miller</TextBlock>
                <ui:TextBlock
                    Grid.Column="2"
                    Grid.RowSpan="2"
                    Text="{Binding ViewModel.AppVersion, Mode=OneWay}"
                    VerticalAlignment="Center"
                />
            </Grid>
        </ui:Card>
    </StackPanel>
</Page>
