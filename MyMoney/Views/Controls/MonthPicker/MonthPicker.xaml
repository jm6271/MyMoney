<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:MyMoney.Views.Controls"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Views/Controls/MonthPicker/MonthPickerButtonStyle.xaml" />
    </ResourceDictionary.MergedDictionaries>


    <local:MonthYearConverter x:Key="MonthYearConverter" />

    <Style TargetType="{x:Type local:MonthPicker}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:MonthPicker}">
                    <Grid>
                        <ToggleButton x:Name="PART_ToggleButton" Style="{StaticResource MonthPickerToggleButtonStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ui:TextBlock FontTypography="Subtitle" Text="{Binding SelectedMonth, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource MonthYearConverter}, ConverterParameter='MMMM yyyy'}" />
                                <ui:SymbolIcon
                                    Grid.Column="1"
                                    Margin="8,0,0,0"
                                    Symbol="CaretDown24" />
                            </Grid>
                        </ToggleButton>
                        <Popup
                            x:Name="PART_Popup"
                            MaxWidth="{TemplateBinding PopupMaxWidth}"
                            AllowsTransparency="True"
                            IsOpen="{Binding IsChecked, ElementName=PART_ToggleButton}"
                            PlacementTarget="{Binding ElementName=PART_ToggleButton}"
                            StaysOpen="False">
                            <Border
                                Margin="12"
                                Background="{DynamicResource ContextMenuBackground}"
                                BorderBrush="{DynamicResource ContextMenuBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Button
                                        x:Name="PART_PrevButton"
                                        Grid.Column="0"
                                        VerticalAlignment="Center"
                                        Content="&#x25C0;" />
                                    <ScrollViewer
                                        x:Name="PART_ScrollViewer"
                                        Grid.Column="1"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Disabled">
                                        <ItemsControl ItemsSource="{Binding Tiles, RelativeSource={RelativeSource TemplatedParent}}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal" />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button
                                                        Padding="0"
                                                        HorizontalAlignment="Left"
                                                        VerticalAlignment="Center"
                                                        HorizontalContentAlignment="Center"
                                                        VerticalContentAlignment="Center"
                                                        Background="Transparent"
                                                        BorderBrush="Transparent"
                                                        BorderThickness="0"
                                                        Cursor="Hand"
                                                        FocusVisualStyle="{DynamicResource DefaultControlFocusVisualStyle}">
                                                        <Button.Template>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border
                                                                    x:Name="MonthTileButtonBorder"
                                                                    Width="{TemplateBinding Width}"
                                                                    Height="{TemplateBinding Height}"
                                                                    Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    CornerRadius="4">
                                                                    <local:MonthTile
                                                                        AccentColor="{Binding AccentColor}"
                                                                        IsCurrentMonth="{Binding IsCurrentMonth}"
                                                                        IsSelected="{Binding IsSelected}"
                                                                        Month="{Binding Month}"
                                                                        TileHeight="{Binding TileHeight}"
                                                                        TileWidth="{Binding TileWidth}" />
                                                                </Border>

                                                                <ControlTemplate.Triggers>
                                                                    <MultiTrigger>
                                                                        <MultiTrigger.Conditions>
                                                                            <Condition Property="IsMouseOver" Value="True" />
                                                                            <Condition Property="IsPressed" Value="False" />
                                                                        </MultiTrigger.Conditions>
                                                                        <Setter TargetName="MonthTileButtonBorder" Property="Background" Value="{DynamicResource LayerOnMicaBaseAltFillColorSecondaryBrush}" />
                                                                    </MultiTrigger>
                                                                    <MultiTrigger>
                                                                        <MultiTrigger.Conditions>
                                                                            <Condition Property="IsMouseOver" Value="True" />
                                                                            <Condition Property="IsPressed" Value="True" />
                                                                        </MultiTrigger.Conditions>
                                                                        <Setter TargetName="MonthTileButtonBorder" Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}" />
                                                                    </MultiTrigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Button.Template>


                                                    </Button>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                    <Button
                                        x:Name="PART_NextButton"
                                        Grid.Column="2"
                                        VerticalAlignment="Center"
                                        Content="&#x25B6;" />
                                </Grid>
                                <Border.Effect>
                                    <DropShadowEffect
                                        BlurRadius="10"
                                        Direction="270"
                                        Opacity="0.135"
                                        ShadowDepth="5"
                                        Color="#202020" />
                                </Border.Effect>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:MonthTile}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:MonthTile}">
                    <Border
                        x:Name="PART_Border"
                        Width="{TemplateBinding TileWidth}"
                        Height="{TemplateBinding TileHeight}"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="2"
                        CornerRadius="4">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock
                                HorizontalAlignment="Center"
                                FontSize="16"
                                FontWeight="SemiBold"
                                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Month, Converter={StaticResource MonthYearConverter}, ConverterParameter=MMM}"
                                TextAlignment="Center" />
                            <TextBlock
                                HorizontalAlignment="Center"
                                FontSize="11"
                                FontWeight="Normal"
                                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Month, Converter={StaticResource MonthYearConverter}, ConverterParameter=yyyy}"
                                TextAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="PART_Border" Property="Background" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=AccentColor}" />
                        </Trigger>
                        <Trigger Property="IsCurrentMonth" Value="True">
                            <Setter TargetName="PART_Border" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=AccentColor}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>