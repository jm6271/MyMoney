<UserControl x:Class="MyMoney.Views.Controls.GroupedComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:MyMoney.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="200">
    <Grid>
        <ui:Button x:Name="dropdownButton" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Padding="10,7"
                   HorizontalContentAlignment="Stretch" Click="dropdownButton_Click">
            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ui:TextBlock Grid.Column="0" HorizontalAlignment="Stretch" Text="{Binding Text,
                RelativeSource={RelativeSource AncestorType=UserControl}}"/>

                <ui:SymbolIcon FontSize="11" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Symbol="ChevronDown12"/>
            </Grid>
        </ui:Button>

        <Popup x:Name="dropDownPopup" PlacementTarget="{Binding ElementName=dropdownButton}" Placement="Bottom"
               StaysOpen="False" Focusable="False" AllowsTransparency="True" Width="{Binding ElementName=dropdownButton, Path=ActualWidth}">
            <Border CornerRadius="8" Background="{DynamicResource LayerOnMicaBaseAltFillColorTertiaryBrush}"
                    BorderThickness="1" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}">
                <ui:ListView Margin="8,12" MaxHeight="250"
                             ItemsSource="{Binding GroupedItems, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             SelectedIndex="{Binding SelectedIndex, RelativeSource={RelativeSource AncestorType=UserControl}}">
                    <ui:ListView.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <ui:TextBlock Margin="4,0,4,0" FontWeight="Bold" Text="{Binding Name}" Foreground="{DynamicResource AccentTextFillColorTertiaryBrush}"/>
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                        </GroupStyle>
                    </ui:ListView.GroupStyle>
                    <ui:ListView.ItemTemplate>
                        <DataTemplate>
                            <ui:TextBlock Margin="4" Text="{Binding Item}"/>
                        </DataTemplate>
                    </ui:ListView.ItemTemplate>
                </ui:ListView>
            </Border>
        </Popup>
    </Grid>

</UserControl>
